manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
  projects:
    - name: zmk
      remote: zmkfirmware
      # Pinned to known-good commit (last working build 2026-04-13).
      # Floating `main` broke nice_view display rendering on both halves.
      revision: 8a44ef109ae3
      import: app/west.yml
  self:
    path: config
